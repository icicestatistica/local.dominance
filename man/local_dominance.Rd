% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local_dominance.R
\name{local_dominance}
\alias{local_dominance}
\title{Teste de Dominância Local entre Duas Distribuições}
\usage{
local_dominance(
  yA,
  yB,
  name_A = "A",
  name_B = "B",
  name_y = "y",
  B = 999,
  quantile = 0.95,
  graficos = TRUE
)
}
\arguments{
\item{yA}{Vetor numérico com os dados do grupo A.}

\item{yB}{Vetor numérico com os dados do grupo B.}

\item{name_A}{Rótulo do grupo A nos gráficos e na saída. Padrão: \code{"A"}.}

\item{name_B}{Rótulo do grupo B nos gráficos e na saída. Padrão: \code{"B"}.}

\item{name_y}{Rótulo do eixo da variável de interesse. Padrão: \code{"y"}.}

\item{B}{Número de permutações (teste omnibus) e de réplicas bootstrap
(concordância). Padrão: \code{999}.}

\item{quantile}{Percentil das distribuições nulas usado como limiar para
selecionar candidatos dominantes. Padrão: \code{0.95}.}

\item{graficos}{Lógico. Se \code{TRUE} (padrão), retorna um objeto \code{patchwork}
com três painéis: densidades, dominância e assertividade.}
}
\value{
Lista com os seguintes elementos:
\describe{
\item{estat_D}{Valor observado da estatística D.}
\item{p_value}{P-valor do teste de permutação omnibus.}
\item{grafico}{Objeto \code{patchwork} (ou \code{NULL} se \code{graficos = FALSE}).}
\item{partition}{Data frame com as faixas de dominância: \code{start}, \code{end},
\code{dominante}, \code{prop_A}, \code{prop_B}, \code{RP} (razão de proporções).}
\item{padrao}{String descrevendo o padrão de dominância (ex.: \code{"A < B"}).}
\item{LI_assertividade}{Limite inferior de assertividade em percentual.}
}
}
\description{
Compara duas amostras usando estimação de densidade kernel (KDE) e um
teste de permutação omnibus. Identifica regiões do suporte onde uma
distribuição domina a outra e estima a assertividade dessa classificação
via bootstrap.
}
\details{
O método opera em três etapas:

\strong{Parte I — Teste omnibus:} estima as densidades de \code{yA} e \code{yB} via KDE
e calcula a diferença \code{g(x) = fA(x) - fB(x)} sobre um grid. A estatística
\code{D = sum(integral_k^2)} agrega as integrais por trecho de sinal. Um teste
de permutação produz o p-valor.

\strong{Parte II — Identificação de regiões dominantes:} se \code{p < 0.05},
compara as integrais acumuladas observadas com o quantil \code{percent_exp} das
distribuições nulas para selecionar os trechos candidatos. Changepoints são
encontrados por interpolação linear nos cruzamentos de \code{g(x) = 0}.

\strong{Parte III — Bootstrap de concordância:} reamostras bootstrap estimam, em
cada ponto do grid, a proporção de réplicas que concorda com o label
atribuído. A assertividade final é o percentual ponderado de pontos com
concordância > 95\%.
}
\examples{
set.seed(42)
yA <- rnorm(200, mean = 0, sd = 1)
yB <- c(rnorm(100, mean = -1, sd = 1), rnorm(100, mean = 2, sd = 1))

resultado <- local_dominance(yA, yB, name_A = "Controle", name_B = "Tratamento")
resultado$p_value
resultado$padrao
resultado$grafico

}
\seealso{
\code{\link[=report_dominance]{report_dominance()}} para imprimir um relatório formatado.
}
